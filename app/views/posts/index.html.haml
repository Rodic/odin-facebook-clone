= form_for(@post) do |f|
  = f.text_area :content
  = f.submit "Post!"

%ul
  - current_user.timeline.each do |post|
    %li
      = link_to(post) do
        = render partial: 'post', locals: { post: post }
      %br
