%h2
  = link_to "Find more friends", users_path

= form_for(@post) do |f|
  = f.text_area :content
  = f.submit "Post!"

%ul
  = render partial: 'post', collection: @posts, layout: 'list_layout'

= will_paginate @posts
