= form_for(@post) do |f|
  = f.text_area :content
  = f.hidden_field :user_id
  = f.submit "Post!"

%ul
  - current_user.timeline.each do |post|
    %li
      %p= post.user.email
      %p= post.content
      %p= post.created_at
      %br
