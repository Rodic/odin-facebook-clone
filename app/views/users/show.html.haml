%h1= @user.email

- if @user != current_user
  = button_to "Add Friend", controller: "friendships", action: "create", user_id: current_user.id, friend_id: @user.id

%h2
  = link_to "Friends", user_friendships_path(@user)
